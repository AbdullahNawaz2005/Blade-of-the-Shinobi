package game;

/**
 * Game states , used enums so that the code can be changed easily
 */
public enum GameState {
    MENU,
    CREDITS,
    PLAYING,
    PAUSED,
    GAME_OVER,
    VICTORY,
    LEVEL_TRANSITION
}
